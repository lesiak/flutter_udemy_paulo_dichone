import 'package:flutter/material.dart';

void main() => runApp(buildWidget0());

Widget buildWidget0() {
  return Center(
    child: Text(
      'Hello World',
      textDirection: TextDirection.ltr,
    ),
  );
}
